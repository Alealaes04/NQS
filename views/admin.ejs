<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Panel de Administración - Nexus Quantum Solutions</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link rel="stylesheet" href="/stylesheets/admin.css">
    <link rel="icon" type="image/png" href="/images/Logo%20Nexus%20Horizon%20sin%20fondo.png">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
</head>
<body>

<%- include('partials/header.ejs') %>

<main class="admin-panel-container">
    <h2>Panel de Administración</h2>
    <p>¡Bienvenido! Aquí puedes gestionar los mensajes recibidos y agregar nuevos proyectos a tu portafolio.</p>

    <a href="/admin/agregar-proyecto" class="cta-button" style="margin: 2rem 0; display: inline-block;">
        Agregar Proyecto
    </a>

    <h3>Mensajes de Contacto</h3>
    <table class="messages-table">
        <thead>
            <tr>
                <th>Fecha y Hora</th>
                <th>Nombre</th>
                <th>Correo Electrónico</th>
                <th>Tipo de Proyecto</th>
                <th>Mensaje</th>
            </tr>
        </thead>
        <tbody>
            <% if (mensajes && mensajes.length > 0) { %>
                <% mensajes.forEach(mensaje => { %>
                    <tr>
                        <td><%= mensaje.timestamp %></td>
                        <td><%= mensaje.name %></td>
                        <td><%= mensaje.email %></td>
                        <td><%= mensaje.projectType %></td>
                        <td><%= mensaje.message %></td>
                    </tr>
                <% }); %>
            <% } else { %>
                <tr>
                    <td colspan="5">No hay mensajes de contacto registrados.</td>
                </tr>
            <% } %>
        </tbody>
    </table>
    
    <div class="projects-section">
      <h3>Proyectos Agregados</h3>
      <table class="messages-table"> <thead>
            <tr>
                <th>ID</th>
                <th>Título</th>
                <th>Descripción Corta</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            <% if (proyectos && proyectos.length > 0) { %>
                <% proyectos.forEach(proyecto => { %>
                    <tr>
                        <td><%= proyecto.id %></td>
                        <td><%= proyecto.title %></td>
                        <td><%= proyecto.shortDescription %></td>
                        <td>
                            <button class="btn-delete" data-id="<%= proyecto.id %>">Eliminar</button>
                        </td>
                    </tr>
                <% }); %>
            <% } else { %>
                <tr>
                    <td colspan="4">No hay proyectos agregados en el portafolio.</td>
                </tr>
            <% } %>
        </tbody>
      </table>
    </div>
</main>

<%- include('partials/footer.ejs') %>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        const deleteButtons = document.querySelectorAll('.btn-delete');
        
        deleteButtons.forEach(button => {
            button.addEventListener('click', async (e) => {
                const projectId = e.target.dataset.id;
                
                if (confirm('¿Estás seguro de que quieres eliminar este proyecto? Esta acción es irreversible.')) {
                    try {
                        const response = await fetch(`/api/proyectos/${projectId}`, {
                            method: 'DELETE'
                        });

                        const result = await response.json();
                        if (result.success) {
                            alert(result.message);
                            window.location.reload(); 
                        } else {
                            alert('Error: ' + result.message);
                        }
                    } catch (error) {
                        console.error('Error al eliminar el proyecto:', error);
                        alert('Ocurrió un error al eliminar el proyecto. Por favor, intente de nuevo.');
                    }
                }
            });
        });
    });
</script>

<script src="/scripts/main.js" defer></script>

</body>
</html>